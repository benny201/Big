<div class="post-content">
  <div class="ui grid">
    <div class="three wide column">
      <a class="avatar"
         href="/posts?author=<%= post.author._id %>"
         data-title="<%= post.author.name %> | <%= ({m: '男', f: '女', x: '保密'})[post.author.gender] %>"
         data-content="<%= post.author.bio %>">
        <img class="avatar" src="/img/<%= post.author.avatar %>">
      </a>
    </div>

    <div class="ten wide column">

      <div class="ui segment content-height">
        <div class="width">
          <h2><a href="/posts/<%= post._id %>"><%= post.title %></a></h2>
          <span style="color: black;">By / <%= post.author.name %></span>
          <br>
          <br>
          <span class="recom-title post-content-span" ><%- post.content %></span>
          <span>......<a href="/posts/<%= post._id%>"><i class="arrow circle right icon"></i>View ALL</a></span>
          <br>
          <br>
          <br>
          <br>
          <span class="tag"><%= post.created_at %></span>
          <span class="tag ">
            <span>浏览(<%= post.pv %>)</span>
            <span>留言(<%= post.commentsCount %>)</span>
            <% if (user && post.author._id && user._id.toString() === post.author._id.toString()) { %>
              <div class="ui inline dropdown">
                <div class="text"></div>
                <i class="dropdown icon"></i>
                <div class="menu">
                  <div class="item"><a href="/posts/<%= post._id %>/edit">编辑</a></div>
                  <div class="item"><a href="/posts/<%= post._id %>/remove">删除</a></div>
                </div>
              </div>
            <% } %>
          </span>

        </div>

        <div class="right">
          <img src="/img/article.jpg" alt="article-img">
        </div>
      </div>



    </div>
  </div>
</div>
